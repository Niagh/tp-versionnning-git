2° La commande la plus utile je trouve que c'est la commande git log pour voir tout ce que j'ai fait, les msgs + les idtfs de commits.
1° J'ai appris à utiliser log, revert, stash, reset, commit --amend. pour gérer l'historique proprement.
3° La partie la plus dur a été d'annuler un commit proprement sans avoir peur de supp l'historique.
Et aussi l’utilisation de `git stash` a été un peu compliquée au début car j’avais une erreur d’index corrompu.
J’ai dû réparer l’index manuellement avant de pouvoir utiliser `stash`.


Commandes utilisées :

git init
git add <fichier>
git commit -m "message"
git log
git show HEAD~1
git diff HEAD~2 HEAD~1
git commit --amend -m "nouveau message"
git reset --soft HEAD~1
git revert <hash ou HEAD~X>
git stash
git stash apply
git push -u origin main